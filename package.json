{
  "name": "gcal-hide-declined",
  "version": "0.1.0",
  "description": "Chrome extension to hide/show declined events on Google Calendar",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.6.0"
  }
}
